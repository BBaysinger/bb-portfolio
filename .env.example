# Example env vars for Docker Compose (loaded from repo root)
# Copy this to `.env` and fill in your values.

# AWS Account ID for private ECR image URIs (used in deploy/compose/docker-compose.yml)
AWS_ACCOUNT_ID=000000000000
