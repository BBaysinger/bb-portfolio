# Example env vars for Docker Compose (loaded from repo root)
# Copy this to `.env` and fill in your values.

# AWS Account ID for ECR image URIs (required for prod services)
AWS_ACCOUNT_ID=000000000000

# EC2 Instance IP address for deployment and SSH access  
EC2_INSTANCE_IP=44.246.43.116
