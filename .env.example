###############################################
# MONOREPO ROOT .env.example
#
# This file is a TEMPLATE.
#
# Use a local, untracked repo-root `.env` when you run:
# - Docker Compose from the repo root (compose variable substitution)
# - Root-level infra/deploy scripts that read env from the repo root
#
# Notes
# - `.env` / `.env.local` are gitignored in this repo.
# - Do NOT put long-lived secrets in committed files.
# - This root `.env` is NOT automatically loaded by Next.js in `frontend/`
#   or the Payload backend in `backend/` when running those apps directly.
#   Each package reads env from its own folder.
###############################################

# Copy this to `.env` (repo root) and fill in your values.

# Precedence (repo root):
# - `.env.local` overrides `.env` (both are gitignored)

# AWS Account ID for ECR image URIs (required for prod services)
AWS_ACCOUNT_ID=<your-aws-account-id> # Required for prod ECR image pulls (no fallback)

# EC2 Instance IP address for deployment and SSH access  
EC2_INSTANCE_IP=<your-elastic-ip>

# AWS region used by infra scripts and bucket tooling
AWS_REGION=us-west-2

# S3 buckets for static project bundles (public + NDA)
# Used by project upload/verify scripts and (in deploy) by infra templates.
PUBLIC_PROJECTS_BUCKET=bb-portfolio-projects-public
NDA_PROJECTS_BUCKET=bb-portfolio-projects-nda

# -----------------------------------------------------------------------------
# MongoDB
# -----------------------------------------------------------------------------
# Base MongoDB connection string(s) WITHOUT the database name.
# The migration script composes the DB-specific URI from these base values.
# Example (Atlas): mongodb+srv://<user>:<pass>@<cluster-host>
# DO NOT include a trailing slash or db name here.
#
# You can set a single default for all envs:
MONGODB_BASE_URI=mongodb+srv://username:password@cluster-host
# Or override per-environment (optional):
# MONGODB_BASE_URI_LOCAL=mongodb://127.0.0.1:27017
# MONGODB_BASE_URI_DEV=mongodb+srv://username:password@cluster-host
# MONGODB_BASE_URI_PROD=mongodb+srv://username:password@cluster-host

# Optional: Override database names per environment if they differ from defaults
# Defaults: bb-portfolio-local, bb-portfolio-dev, bb-portfolio-prod
# MONGODB_DB_NAME_LOCAL=bb-portfolio-local
# MONGODB_DB_NAME_DEV=bb-portfolio-dev
# MONGODB_DB_NAME_PROD=bb-portfolio-prod
