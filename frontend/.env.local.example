###############################################
# FRONTEND LOCAL TEMPLATE (.env.local.example)
# Copy to .env.local and adjust as needed. Do NOT commit .env.local.
#
# This file configures the *frontend Next.js app*.
# It is read when you run commands in `frontend/` (e.g. `npm run dev`).
#
# Precedence (frontend folder):
# - `.env.local` overrides `.env`
###############################################

# App port (Next.js dev server)
PORT=3000

# Internal backend URL for server-side fetches
# If using docker-compose local profile: http://bb-portfolio-backend-local:3001
# If running backend on host:           http://localhost:3001
#
# Used by frontend route handlers + proxy routes to talk to Payload.
BACKEND_INTERNAL_URL=http://localhost:3001

# Optional: explicit profile hint used by some helpers.
# Recommended values: local | dev | prod
ENV_PROFILE=local

# S3 project bundle buckets (used by Next.js route handlers)
# - Public projects are streamed via `/projects/...`
# - NDA projects are streamed via `/private/...` (auth-gated via backend session)
PUBLIC_PROJECTS_BUCKET=bb-portfolio-projects-public
NDA_PROJECTS_BUCKET=bb-portfolio-projects-nda

# Optional prefixes within the buckets (folder paths)
# Example: if objects are under `public/` then set PUBLIC_PROJECTS_PREFIX=public
# PUBLIC_PROJECTS_PREFIX=
# NDA_PROJECTS_PREFIX=

# S3 region for AWS SDK clients.
# The route handlers check `AWS_REGION` then `AWS_DEFAULT_REGION`.
AWS_REGION=us-west-2

# Optional: frontend runtime health check override (helps when frontend runs on host and backend is not proxied)
# Leave unset when using compose/caddy so relative /api/health/ works through the proxy.
# NEXT_PUBLIC_HEALTH_URL=http://localhost:3001/api/health/

# React StrictMode control (optional)
# Default behavior by profile:
#   - ENV_PROFILE=local          -> StrictMode ON (helps catch issues while developing locally)
#   - ENV_PROFILE=dev|development -> StrictMode OFF (avoid double-invocation in dev deployments)
#   - ENV_PROFILE=prod|production -> StrictMode OFF
# Override with REACT_STRICT_MODE to force behavior regardless of profile:
#   - REACT_STRICT_MODE=false   # force off
#   - REACT_STRICT_MODE=true    # force on
# REACT_STRICT_MODE=true

# Google Analytics 4 (optional)
# Set to your GA4 Measurement ID (looks like "G-XXXXXXXXXX").
# NEXT_PUBLIC_GA_MEASUREMENT_ID=
