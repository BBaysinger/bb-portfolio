###############################################
# FRONTEND LOCAL TEMPLATE (.env.local.example)
# Copy to .env.local and adjust as needed. Do NOT commit .env.local.
# Precedence: .env.local overrides .env
###############################################

# App port (Next.js dev server)
PORT=3000

# Prefixed backend URLs for local development (no backwards compatibility)
# If using docker-compose local profile: http://backend-local:3001
# If running backend on host: http://localhost:3001
LOCAL_BACKEND_INTERNAL_URL=http://localhost:3001
LOCAL_NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Next.js requires NEXT_PUBLIC_ variables for browser exposure
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# React StrictMode control (optional)
# Set to "false" to disable React StrictMode (useful for debugging double-renders)
# REACT_STRICT_MODE=false
