{
  // Package name for the Payload backend
  "name": "bb-portfolio-backend",
  // Version of this backend package
  "version": "1.0.17",
  // Short description of backend package purpose
  "description": "A blank template to get started with Payload 3.0",
  // License type (MIT is very permissive)
  "license": "MIT",
  // Declares this as an ES module project
  "type": "module",
  // Common dev scripts
  "scripts": {
    // Prettier + Build: Format all files with Prettier, then run TypeScript build to validate
    // This approach helps to ensure that formatting doesn't break the build, because that does happen sometimes
    // The command is short for frequent use (I use it very frequently, and *every* time before commit)
    "precommit": "prettier --write . && tsc -b",
    // Validate required environment variables before building
    "prebuild": "tsx ./scripts/check-required-env.ts",
    // Validate required environment variables before starting dev server
    "predev": "tsx ./scripts/check-required-env.ts && npm run generate:importmap",
    // Production build with increased memory and deprecation warnings suppressed
    "build": "cross-env NODE_OPTIONS=\"--no-deprecation --max-old-space-size=8000\" npm run generate:importmap && next build --webpack",
    // Start development server with deprecation warnings suppressed
    "dev": "cross-env NODE_OPTIONS=--no-deprecation npm run generate:importmap && next dev -p ${PORT:-3001}",
    // Remove the Next build cache before starting the backend dev server.
    "dev:clean": "rm -rf .next && cross-env NODE_OPTIONS=--no-deprecation next dev -p ${PORT:-3001}",
    // Generates Payload importmap file (used when importing from plugins or dynamic code)
    "generate:importmap": "cross-env NODE_OPTIONS=--no-deprecation payload generate:importmap",
    // Generates TypeScript types from Payload collections and globals
    "generate:types": "cross-env NODE_OPTIONS=--no-deprecation payload generate:types",
    // Export CV experience global content into the external seedings folder.
    "export:cv-content": "cross-env NODE_OPTIONS=--no-deprecation tsx ./scripts/export-cv-experience-content.ts",
    // Export project description HTML blocks into the external seedings folder.
    "export:project-descriptions": "cross-env NODE_OPTIONS=--no-deprecation tsx ./scripts/export-project-descriptions-content.ts",
    // Import CV experience content from the external seedings folder into Payload
    "import:cv-content": "cross-env NODE_OPTIONS=--no-deprecation tsx ./scripts/import-cv-experience-content.ts",
    // Import CV experience content into the local backend environment
    "import:cv-content:local": "npm run import:cv-content -- --env local",
    // Import CV experience content into the dev backend environment
    "import:cv-content:dev": "npm run import:cv-content -- --env dev",
    // Import project description HTML blocks from the external seedings folder into Payload
    "import:project-descriptions": "cross-env NODE_OPTIONS=--no-deprecation tsx ./scripts/import-project-descriptions-content.ts",
    // Import project descriptions into the local backend environment
    "import:project-descriptions:local": "npm run import:project-descriptions -- --env local",
    // Import project descriptions into the dev backend environment
    "import:project-descriptions:dev": "npm run import:project-descriptions -- --env dev",
    // Seed CV experience data into Payload using the shared seed script
    "seed:cv-experience": "cross-env NODE_OPTIONS=--no-deprecation tsx ./scripts/seed-cv-experience.ts",
    // Seed CV experience data into the local backend environment
    "seed:cv-experience:local": "npm run seed:cv-experience -- --env local",
    // Seed CV experience data into the dev backend environment
    "seed:cv-experience:dev": "npm run seed:cv-experience -- --env dev",
    // Standalone smoke test for the Payload admin login flow
    "smoke:admin-login": "node scripts/smoke-admin-login.mjs",
    // Run Next.js linter with deprecation warnings suppressed
    "lint": "cd .. && eslint backend",
    // Start Payload-only CLI (e.g., to seed or run utilities)
    "payload": "cross-env NODE_OPTIONS=--no-deprecation payload",
    // Validate required environment variables before starting production server
    "prestart": "tsx ./scripts/check-required-env.ts",
    // Start production server
    "start": "cross-env NODE_OPTIONS=--no-deprecation next start",
    // Run both integration and E2E tests (sequential)
    "test": "pnpm run test:int && pnpm run test:e2e",
    // Run Playwright tests (end-to-end testing framework)
    "test:e2e": "cross-env NODE_OPTIONS=\"--no-deprecation --no-experimental-strip-types\" pnpm exec playwright test",
    // Run Vitest (unit/integration tests) using its config file
    "test:int": "cross-env NODE_OPTIONS=--no-deprecation vitest run --config ./vitest.config.mts",
  },
  // Runtime dependencies for backend and CMS functionality
  "dependencies": {
    // AWS SES client used for sending transactional emails
    "@aws-sdk/client-ses": "^3.1037.0",
    // Shared AWS SDK types
    "@aws-sdk/types": "^3.973.8",
    // MongoDB adapter for Payload CMS
    "@payloadcms/db-mongodb": "3.84.1",
    // Nodemailer email adapter for Payload CMS (handles SMTP email sending, password resets, notifications)
    "@payloadcms/email-nodemailer": "3.84.1",
    // Enables Payload-Next.js integration (server-side rendering and routing)
    "@payloadcms/next": "3.84.1",
    // Payload Cloud tools (optional if self-hosting)
    "@payloadcms/payload-cloud": "3.84.1",
    // Rich text editor using Lexical (modern alternative to Slate)
    "@payloadcms/richtext-lexical": "3.84.1",
    // S3-backed media storage for Payload uploads
    "@payloadcms/storage-s3": "3.84.1",
    // Payload's core UI components
    "@payloadcms/ui": "3.84.1",
    // BSON/EJSON utilities used by database migration and snapshot scripts
    "bson": "^7.2.0",
    // Cross-platform environment variable setting (Windows/Unix compatibility)
    "cross-env": "^10.1.0",
    // Environment variable loader (reads .env files into process.env)
    "dotenv": "17.4.2",
    // Express.js web server framework (v5 for latest features)
    "express": "^5.2.1",
    // GraphQL query language runtime (required by Payload's GraphQL API)
    "graphql": "^16.13.2",
    // YAML parser used by content tooling and import/export helpers
    "js-yaml": "^4.1.1",
    // JSON5 parser for annotated configuration files
    "json5": "^2.2.3",
    // Direct MongoDB client used by database sync scripts
    "mongodb": "~7.2.0",
    // Next.js framework for frontend/backend integration
    "next": "16.2.4",
    // Payload CMS core package
    "payload": "3.84.1",
    // React version to match Next.js 15
    "react": "19.2.5",
    // React DOM library for rendering React components (required by Next.js and Payload UI)
    "react-dom": "19.2.5",
    // High-performance image processing (required for Payload image fields)
    "sharp": "0.34.5",
    // Converts strings into URL-friendly slugs (useful for dynamic routing)
    "slugify": "^1.6.9",
  },
  // Development-only tools and type definitions
  "devDependencies": {
    // E2E test runner
    "@playwright/test": "1.59.1",
    // Testing utilities for React components
    "@testing-library/react": "16.3.2",
    // Express.js TypeScript definitions
    "@types/express": "^5.0.6",
    // TypeScript definitions for js-yaml used by private content import helpers
    "@types/js-yaml": "^4.0.9",
    // Node.js TypeScript definitions (latest for Node 18+)
    "@types/node": "^25.6.0",
    // React 19 types
    "@types/react": "19.2.14",
    // React DOM 19 types
    "@types/react-dom": "19.2.3",
    // Vite React plugin (enables React support in Vitest)
    "@vitejs/plugin-react": "6.0.1",
    // Maps baseline browser support targets for tooling that needs them
    "baseline-browser-mapping": "^2.10.22",
    // ESLint JavaScript/TypeScript linter (v9 for latest rules)
    "eslint": "^9.39.4",
    // Next.js ESLint configuration (includes React and accessibility rules)
    "eslint-config-next": "16.2.4",
    // Browser DOM simulation for Node.js testing environments
    "jsdom": "29.0.2",
    // Standalone version of Playwright test runner
    "playwright": "1.59.1",
    // Playwright core library (used for browser automation)
    "playwright-core": "1.59.1",
    // Code formatter with opinionated defaults
    "prettier": "^3.8.3",
    // TSX lets us run TypeScript build/utility scripts without compiling first.
    "tsx": "^4.21.0",
    // TypeScript compiler (v5.9 for latest language features)
    "typescript": "6.0.3",
    // Vite path mapping support (resolves TypeScript path aliases)
    "vite-tsconfig-paths": "6.1.1",
    // Fast unit test runner (modern Jest alternative)
    "vitest": "4.1.5",
  },
  // Pin patched versions of transitive dependencies.
  // Payload pulls in nodemailer via @payloadcms/email-nodemailer and @payloadcms/payload-cloud.
  // `npm audit` flags <= 7.0.10; override to the latest patch.
  "overrides": {
    // Keep this pinned until Payload updates its nodemailer dependency.
    // If `npm audit` stops flagging nodemailer (or Payload bumps it), remove this override.
    "nodemailer": "7.0.12",
  },
  // Minimum versions for runtime tools
  "engines": {
    // Node 18.20.2+ or 20.9.0+ (for ES modules and latest security patches)
    "node": "^18.20.2 || >=20.9.0",
    // Pin npm 11 so lockfile writes stay consistent across local/dev/prod installs.
    "npm": ">=11 <12",
    // PNPM v9 or v10 (for workspace support and faster installs)
    "pnpm": "^9 || ^10",
  },
  // Only build native dependencies that need precompilation to make CI faster
  "onlyBuiltDependencies": [
    "sharp",
    "esbuild",
    "unrs-resolver",
  ],
}