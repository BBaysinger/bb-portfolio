# CV Experience Seed Logos

This folder stores source logo files used by `backend/scripts/seed-cv-experience.ts`.

Notes:

- These files are seed inputs, not frontend public assets.
- `bb.svg` is duplicated here because `/images/cv/bb.svg` is still used by the live CV page outside of CMS-driven experience blocks.
