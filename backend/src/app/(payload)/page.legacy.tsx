// Legacy temporary redirect removed — preserved for reference.
// This module is intentionally not a page to avoid route conflicts
// with the generated admin pages located under
// `src/app/(payload)/[[...segments]]/page.tsx`.

export const legacy = true
