// Frontend welcome page retired. Kept in tree for reference only.
// This module intentionally exports no default page, preventing
// Next from registering it as a route and avoiding a collision
// with the admin pages served under `(payload)`.

export const retiredFrontendPage = true
import { fileURLToPath } from 'url'

import { headers as getHeaders } from 'next/headers.js'
import Image from 'next/image'
import Link from 'next/link'
import { getPayload } from 'payload'
import React from 'react'

import config from '@/payload.config'
import './styles.css'

// Frontend welcome page retired. Kept in tree for reference only.
// This module intentionally exports no default page, preventing
// Next from registering it as a route and avoiding a collision
// with the admin pages served under `(payload)`.

export const retiredFrontendPage = true
