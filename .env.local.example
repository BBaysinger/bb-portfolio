
###############################################
# MONOREPO ROOT .env.local.example
#
# Optional override file for root-level scripts.
#
# This repo ignores `.env` and `.env.local`, so both are safe to keep local.
# Use `.env` for "shared" local defaults and `.env.local` for personal/secret overrides.
###############################################

# Precedence (repo root):
# - `.env.local` overrides `.env`

# Reminder:
# - Root env files are used by Docker Compose / infra scripts.
# - They are NOT automatically used by `frontend/` or `backend/` when running those apps directly.

# Example: keep sensitive Mongo credentials in .env.local instead of .env
# MONGODB_BASE_URI=mongodb+srv://username:password@cluster-host

# Example: override AWS region for your account/buckets
# AWS_REGION=us-west-2

# Example: override buckets if you use alternate stacks
# PUBLIC_PROJECTS_BUCKET=bb-portfolio-projects-public
# NDA_PROJECTS_BUCKET=bb-portfolio-projects-nda

